# Single-app (no OTA) layout for the 16 MB flash. The factory app is generous because the
# LVGL UI + audio codec + WiFi/TLS stack is large; `storage` (spiffs) holds future assets.
# Name,     Type, SubType, Offset,   Size,     Flags
nvs,        data, nvs,     0x9000,   0x6000,
phy_init,   data, phy,     0xf000,   0x1000,
factory,    app,  factory, 0x10000,  0x400000,
storage,    data, spiffs,  ,         0x200000,

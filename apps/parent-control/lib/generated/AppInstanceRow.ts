// This file was generated by [ts-rs](https://github.com/Aleph-Alpha/ts-rs). Do not edit this file manually.
import type { AppInstallBindings } from "./AppInstallBindings";
import type { AppInstanceStatus } from "./AppInstanceStatus";
import type { Availability } from "./Availability";
import type { BoundChannel } from "./BoundChannel";

/**
 * One `app instance` row (arch.md §5): template@version ⊗ the master's
 * bindings ⊗ the minted grant set ⊗ its delegate.
 */
export type AppInstanceRow = { 
/**
 * The delegate label (unique per operator) — the instance id.
 */
label: string, template_id: string, template_version: string, template_schema: number, 
/**
 * The delegate's HDKD child omni (`0x`).
 */
actor_omni: string, device_key_hash: string, memory_ns: string, chat_channel_id: string, bindings: AppInstallBindings, bound_channels: Array<BoundChannel>, 
/**
 * The delegate's granted service NAMES at install (the sheet).
 */
services: Array<string>, availability: Availability, status: AppInstanceStatus, installed_at: number, uninstalled_at?: number, 
/**
 * #425 O4 — the keep-vs-delete choice recorded at uninstall.
 */
resources_kept?: boolean, 
/**
 * The aliases the install wrote into contacts' `reach` (undone at
 * uninstall) — one per messaging slot, normally just the label.
 */
reach_aliases: Array<string>, };

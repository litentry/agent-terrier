# Deployed contracts — moved

The canonical deployed-contract registry now lives at **[`docs/spec/deployed-contracts.md`](spec/deployed-contracts.md)** — single source for every on-chain address across chains plus the prod/test EVM deployer wallets, indexed from [`arch.md`](arch.md) §5.

This file is kept only as a redirect so links added in #171 still resolve. Do not add addresses here — edit [`docs/spec/deployed-contracts.md`](spec/deployed-contracts.md).
